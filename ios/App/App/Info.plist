<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>BibleQuest</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>3</string>
	<!-- Declared false because every cryptographic path here is exempt: HTTPS
	     through the system TLS stack, one WebCrypto SHA-256 digest (a hash, not
	     encryption), and Supabase PKCE/JWT handling, which is authentication.
	     No cryptographic library ships in the bundle. Without this key every
	     upload parks in "Missing Compliance" and cannot reach even an internal
	     tester until the questionnaire is answered by hand. Re-verify against
	     Apple's exemption criteria if the app ever encrypts data itself. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
    <false/>
    <key>UISceneConfigurations</key>
    <dict>
      <key>UIWindowSceneSessionRoleApplication</key>
      <array>
        <dict>
            <key>UISceneConfigurationName</key>
            <string>Default Configuration</string>
            <key>UISceneDelegateClassName</key>
            <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
            <key>UISceneStoryboardFile</key>
            <string>Main</string>
        </dict>
      </array>
    </dict>
	</dict>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<!-- The Capacitor template ships armv7 here, which is a 32-bit capability no
	     device running this app's iOS 15 minimum can report. arm64 is the
	     accurate requirement. -->
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<!-- Portrait only on iPhone, matching `orientation: "portrait"` in
	     src/app/manifest.ts. The reading layout is built around a single
	     column; iPad keeps all orientations below, where the width is useful. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
	<!-- No NSCameraUsageDescription, NSMicrophoneUsageDescription,
	     NSLocationWhenInUseUsageDescription, NSContactsUsageDescription or
	     NSPhotoLibraryUsageDescription appear here on purpose. A local bundle
	     sends no Permissions-Policy header, so the 14 feature denials in
	     next.config.ts have no equivalent on iOS — but iOS cannot grant a
	     capability the app never declares, which achieves the same result.
	     Adding any of these keys re-opens a capability the web app denies. -->
</dict>
</plist>
