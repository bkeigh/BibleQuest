import { PrayerScreen } from "@/components/prayer/PrayerScreen";

export const metadata = { title: "Prayer Journal — Reflections" };

export default function PrayerReflectionsPage() {
  return <PrayerScreen />;
}
